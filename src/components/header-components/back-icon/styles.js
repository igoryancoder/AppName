import { StyleSheet } from 'react-native';
import { colors } from '../../../constants';

export default StyleSheet.create({
	backIconContainer: {
		alignItems: 'center',
		flexDirection: 'row',
	},
	backIcon: {
		color: colors.dark,
		fontSize: 22,
		fontFamily: 'fontello',
	},
});
